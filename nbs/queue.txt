- utility.markdown
    - replace_all_links_with_formatted_links
        - # TODO make it so that if a link is already specified with a format, then the format
          # is preserved.
          # TODO make it so that if a link is a url, then the url is preserved.